describe('Tests index.html page', () => {
    before('', () => {

    });
    it('', () => {
        elementsDom.inputValues.click();

        expect(elementsDom.inputValues.value).to.be.equal(null);
    });
});